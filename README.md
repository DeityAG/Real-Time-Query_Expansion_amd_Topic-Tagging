# Real-Time-Query_Expansion_amd_Topic-Tagging
End-to-end Streamlit app that watches a live conversation and for every user message:  Expands implicit queries (pronouns, ellipsis, topic switches) into self-contained questions Tags each message with a 2-level topic label (e.g. Politics > India)
